
### About
Name: Arthur Chen
EID: ac93957

### Compilation
You can compile this program by running the ```build.sh``` file in the top directory, and it will generate the ```hw4``` executable in the top directory. 

### Execution
After compiling the program, you can run it by changing your directory to that of the program, then running ```./programNameHere inputPathHere```

### Testing
To compile and run the tests, first run the command ```gcc tests.c instructionFunctions.c -o tests```

Then, run ```./tests```